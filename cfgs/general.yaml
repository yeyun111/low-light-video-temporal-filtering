# This file contain the default configuration for the low light video temporal filtering
pipe:
  - filter: wiener
    window_size: [19, 1, 1, 1]
  - filter: dct
    kernel_size: [3, 1, 1]
    threshold: 0.1

frames_per_clip: 36
